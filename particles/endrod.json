{
  "format_version": "1.21.60",
  "particle_effect": {
    "description": {
      "identifier": "minecraft:endrod",
      "basic_render_parameters": {
        "material": "particles_blend",
        "texture": "textures/particle/particles"
      }
    },
    "components": {
      "minecraft:emitter_initialization": {
        "creation_expression": "v.drift_phase = math.random(0, 6.28); v.sway_rate = math.random(0.8, 1.6); v.float_dir = v.particle_random_3 > 0.5 ? 1.0 : -1.0;"
      },
      "minecraft:emitter_rate_manual": {
        "max_particles": 120
      },
      "minecraft:emitter_lifetime_expression": {
        "activation_expression": 1,
        "expiration_expression": 0
      },
      "minecraft:emitter_shape_point": {
        "direction": [
          "math.random(-0.15, 0.15)",
          -1.0,
          "math.random(-0.15, 0.15)"
        ]
      },
      "minecraft:particle_initial_speed": "math.random(0.0, 0.06)",
      "minecraft:particle_lifetime_expression": {
        "max_lifetime": "4.5 + v.particle_random_1 * 2.5"
      },
      "minecraft:particle_motion_dynamic": {
        "linear_acceleration": [
          "math.sin(v.sway_rate * v.particle_age + v.drift_phase) * 0.055 * v.float_dir",
          "-0.055 * (1.0 - math.clamp(v.particle_age / (4.5 + v.particle_random_1 * 2.5), 0, 1) * 0.55)",
          "math.cos(v.sway_rate * v.particle_age + v.drift_phase + 1.57) * 0.045 * v.float_dir"
        ],
        "linear_drag_coefficient": 0.72
      },
      "minecraft:particle_appearance_billboard": {
        "size": [
          "math.lerp(0.14 + v.particle_random_1 * 0.10, 0.0, math.pow(math.clamp(v.particle_age / (4.5 + v.particle_random_1 * 2.5), 0, 1), 2.4)) * math.lerp(0.0, 1.0, math.clamp(v.particle_age / 0.12, 0, 1))",
          "math.lerp(0.14 + v.particle_random_1 * 0.10, 0.0, math.pow(math.clamp(v.particle_age / (4.5 + v.particle_random_1 * 2.5), 0, 1), 2.4)) * math.lerp(0.0, 1.0, math.clamp(v.particle_age / 0.12, 0, 1))"
        ],
        "facing_camera_mode": "lookat_xyz",
        "uv": {
          "texture_width": 128,
          "texture_height": 128,
          "flipbook": {
            "base_UV": [ 56, 88 ],
            "size_UV": [ 8, 8 ],
            "step_UV": [ -8, 0 ],
            "frames_per_second": 6,
            "max_frame": 8,
            "stretch_to_lifetime": true,
            "loop": false
          }
        }
      },
      "minecraft:particle_appearance_tinting": {
        "color": [
          "math.lerp(1.00, 0.49, math.pow(math.clamp(v.particle_age / (4.5 + v.particle_random_1 * 2.5), 0, 1), 1.2))",
          "math.lerp(0.92, 0.33, math.pow(math.clamp(v.particle_age / (4.5 + v.particle_random_1 * 2.5), 0, 1), 1.6))",
          "math.lerp(1.00, 0.72, math.pow(math.clamp(v.particle_age / (4.5 + v.particle_random_1 * 2.5), 0, 1), 0.8))",
          "math.lerp(0.95, 0.0, math.pow(math.clamp(v.particle_age / (4.5 + v.particle_random_1 * 2.5), 0, 1), 2.8)) * math.lerp(0.0, 1.0, math.clamp(v.particle_age / 0.12, 0, 1))"
        ]
      }
    }
  }
}
